from models.models import LSTM, AE_LSTM
import json
from utils.data_utils import build_tokenizer, build_embedding_matrix, SentenceDataset

class Predictor:
    def __init__(self, model_name='AE'):
        self.model_name = model_name

    def build_prerequisites(self):


    def build_model(self):
        model =

    def load_weights(self):



